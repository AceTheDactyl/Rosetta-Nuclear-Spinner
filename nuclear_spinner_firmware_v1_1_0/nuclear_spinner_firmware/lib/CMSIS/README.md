# CMSIS Placeholder

This repository is self-contained for demonstration, but the embedded Makefile
assumes CMSIS headers are available under:

- `lib/CMSIS/Include`
- `lib/CMSIS/Device/ST/STM32H7xx/Include`

For a real STM32H743 build, vendor CMSIS + STM32CubeH7 HAL/LL should be placed here.
